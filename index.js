// Archivo principal que deberá ser ejecutado para interactuar con la
// aplicación. En este archivo se deberán importar las operaciones que creen y lean las
// citas de atención en la veterinaria.

const { registrar, leer } = require ("./operaciones");

if (operacion == "registrar") {
    registrar(nombre, edad, tipoAnimal, colorAnimal, enfermedad);
    }
    if ( operacion == "leer"){
        leer();
    }